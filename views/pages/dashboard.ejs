<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

<style>
    .dataTables_actions {
        display: none;
    }
    .dataTables_length select option {
        background: #192c47;
    }
    .page-select option {
        background: #160e1c;
    }
</style>
<body data-sa-theme="2">
<%- include('../partials/header') %>
<%- include('../partials/sidebar') %>

<section class="content">
    <header class="content__title">
        <h1>Dashboard</h1>
    </header>
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table id="stock_table" class="table table-bordered table-striped text-center mb-0">
                    <thead>
                    <tr>
                        <th style="width: 5%">No</th>
                        <th style="width: 10%">Name</th>
                        <th style="width: 25%">Address</th>
                        <th style="width: 40%">Bio</th>
                        <th style="width: 20%">True/False</th>
                    </tr>
                    </thead>
                    <tbody>
                    <% for (let i = 0; i < contacts.length; i++) { %>
                        <tr>
                            <td><%= i + 1 %></td>
                            <td><%= contacts[i].name %></td>
                            <td><%= contacts[i].address %></td>
                            <td><%= contacts[i].bio %></td>
                            <td><%= contacts[i].isApproved %></td>
                        </tr>
                    <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>

<%- include('../partials/footer') %>
<%- include('../partials/foot') %>

<script>
    $(function () {
        $('#stock_table').DataTable({
            aaSorting: [],
            autoWidth: !1,
            responsive: !0,
            lengthMenu: [[15, 40, 100, -1], ['15 Rows', '40 Rows', '100 Rows', 'Everything']],
            language: {searchPlaceholder: 'Search for records...'},
            sDom: '<"dataTables__top"flB<"dataTables_actions">>rt<"dataTables__bottom"ip><"clear">'
        });
    });
</script>
</body>
</html>
